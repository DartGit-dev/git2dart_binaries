# Validation Questions — git2dart_binaries

> Reviewer refresh: 2026-08-25
> Mode: chat

## Pending questions

No new user-answerable questions remain.

The current red items are missing hosted-run, artifact-identity, device, publication, or cross-repository observations, plus repository-visible implementation defects. Answering them requires evidence or implementation, not another stakeholder preference.

## Revalidated prior decisions

| Prior question | Preserved decision | Current revalidation |
|---|---|---|
| Q1 Validator contract | Reconstruct complete Git-valid ref names and finite object types; do not preserve permissive predicates. | Current `extensions.dart` still contains the permissive implementation, so it remains an observed defect and the target policy remains valid. |
| Q2 Lifecycle ownership | Preserve the managed runtime and public shutdown compatibility; add no automatic teardown policy. | Current `runtime.dart` still centralizes init/shutdown and pin accounting. |
| Q3 Windows missing bundle | Fail as an incomplete package; do not retry a bare dependency name. | Current source still fails indirectly through the required package path; the explicit target remains valid. |
| Q4 Global-options ABI | Official libgit2 1.9.6 headers plus same-run CI-generated untracked bindings are authoritative; complete native coverage is required. | `bindings.dart` remains absent/untracked and current native completion evidence is unavailable. |
| Q5 Android TLS | External bootstrap owns init → extract → apply; Android/iOS first calls must share one in-flight operation. | Local extraction/retry is current and tested; external implementation/device execution was not re-inspected. |
| Q6 Version synchronization | Pub and both Apple podspec versions must match exactly; divergence blocks release. | Current values still diverge: 1.12.1 versus 1.11.2/1.11.2. |
| Q7 Windows OpenSSL | Build the explicit pin from source, or prove an approved exact-parity exception. | Current Windows action now source-builds the declared OpenSSL input; hosted payload/parity evidence is still absent. |
| Q8 Cross-repository coordination | `git2dart` owns selected-pair integration coordination; binaries owns the libgit2 pin. | Preserved as user-confirmed policy; no current external workflow/run was inspected. |
| Q9 Release routing | Feature runs are merge-only; a separate green exact-main run gates publication. | Current parsed workflow preserves broad validation and exact-main publisher reachability; hosted execution is unobserved. |
| Q10 Publication controls | Existing external GitHub/pub.dev controls are accepted; no additional supply-chain controls are requested now. | Preserved as user-confirmed external configuration, not repository-visible proof. |

## Interview status

- Previously generated: 10
- Previously answered: 10
- New questions generated by this refresh: 0
- Questions awaiting user input: 0
