# Report intake: aggregate platform-proof validation regression

## Original report

"Feature 003-platform-release-proof W002 is red. The aggregate validator accepts empty inventory and versions and a null attestation without joining proof hashes to the payload."

## Observations

- The 2026-08-25 semantic regression check re-extracted 23 watch items: 22 green and one red.
- The red item is `003-platform-release-proof/W002`.
- The effective feature watch requires each final payload report to inventory expected artifact-relative paths, SHA-256 values, sizes, and a platform loader or linkage result.
- Fresh extraction records that a proof with `status=passed`, empty inventory and versions, and null attestation can pass aggregate validation.
- Fresh extraction also records that proof hashes are not compared with the downloaded release payload bytes.
- Confidence re-extraction is complete. No runtime or hosted execution is claimed by this intake.

## Expected behavior

Aggregate platform-proof validation rejects semantically empty or incomplete passed records and verifies that recorded digests identify the assembled payload bytes before release eligibility can advance.

## Observed behavior

Aggregate validation accepts structurally present but semantically empty records and does not establish the proof-to-payload identity join.

## Reproduction and frequency

- The feature fixture deliberately supplies passed records with empty inventory and versions and a null attestation.
- The aggregate implementation accepts those omissions.
- Classification: deterministic from static source and fixture evidence.
- Rate: one observed semantic regression out of 23 re-extracted watch items.

## Inferred context

`validation-release-assembly`, matching the existing context and taxonomy mapping `build-release / ci-supply-chain / validation-release-assembly`.

## Dedupe candidate

`BUG-20260817-AAGV`, "Release assembly does not enforce native inventory", is a probable overlap because it covers missing final native-inventory enforcement and trusted checksum or manifest validation. The new report is broader in one direction because it also names version, linkage, attestation semantics, and a direct hash join between proof records and downloaded payload bytes. The dedupe disposition requires a user choice before canonical registration.

## Classification

- Severity: `high`
- Priority: `P1`
- The user confirmed both values on 2026-08-25. They match the existing aggregate classification of `BUG-20260817-AAGV`.
